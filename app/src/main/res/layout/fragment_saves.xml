<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
  android:layout_height="match_parent"
    >

  <RelativeLayout
      android:id="@+id/header_saved"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:background="@color/colorText"
      >
    <TextView
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:text="Saved"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="20dp"
        android:gravity="center"
        ></TextView>
  </RelativeLayout>

  <RelativeLayout
      android:id="@+id/saved_show_no_Order"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_centerVertical="true"
      >

    <ImageView
        android:id="@+id/logo_saved_show_noOrder"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/saved" />

    <TextView
        android:id="@+id/title_saved_show_noOrder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No Saved Orders"
        android:layout_marginTop="10dp"
        android:layout_below="@id/logo_saved_show_noOrder"
        android:textAlignment="center"
        android:textSize="15dp"
        android:textFontWeight="400"
        android:textColor="@color/colorAccent"
        ></TextView>
  </RelativeLayout>

  <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/list_saves_product_holder_View"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:paddingLeft="5dp"
      android:paddingRight="5dp"
      android:layout_marginTop="2dp"
      android:layout_below="@+id/header_saved"
      ></androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>